import React, {FC} from 'react';
import {Text} from './styles';

export const UnderConstruction: FC = () => {
  return <Text>Página em construção! Volte em breve ;)</Text>;
};

export default UnderConstruction;
